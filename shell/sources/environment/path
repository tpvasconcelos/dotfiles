########################################
# Add some bins to PATH
########################################
path=(
  "${BREW_PREFIX}/sbin"
  "${HOME}/.flutter/bin"
  "${HOME}/.poetry/bin"
  "${GOPATH}/bin"
  "${path[@]}"
)
export PATH
typeset -U PATH path
