log_info() {
  faint "❱ ${*}" >&2
}

log_info() {
  bold "$(fg_cyan "❱ ${*}")" >&2
}

log_warning() {
  bold "$(fg_yellow "✕ ${*}")" >&2
}

log_error() {
  bold "$(fg_red "✖ ${*}")" >&2
}

log_success() {
  bold "$(fg_green "✔ ${*}")" >&2
}
