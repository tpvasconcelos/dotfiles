contains() {
  # test if a string contains a substring
  local string substring
  string="$1"
  substring="$2"
  if test "${string#*$substring}" != "$string"
  then
    return 0
  else
    return 1
  fi
}
